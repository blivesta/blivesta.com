import React from 'react';
// import styled from 'styled-components';
import { storiesOf } from '@storybook/react';
import NotFound from '.';

storiesOf(`organisms|NotFound`, module).add(`default`, () => <NotFound />);
